package ru.job4j.tasksortdeparament;//package ru.job4j.tasksortdeparament;

//public class DepartmentTask {

	//public static class Depart {
	//	List<String> names = new ArrayList<>();

		//public Depart(List<String> names) {
		//	this.names = names;
	//	}

		//public static void main(String[] args) {
			//Depart left = new Depart(Arrays.asList("k2"));
			//Depart right = new Depart(Arrays.asList("k2", "sk2"));
			//compare(left, right);
	//	}

	//	public static int compare(Depart left, Depart right) {*/
//			int res = 0;

			//while (!left.equals(right)) {
				// int s = Math.abs(left.names.size() - right.names.size());
				//boolean isLengthsEqual = left.names.size() - right.names.size() == 0 ? true : false;
				// Запоминаем длину списока с наименьшим количеством подразделений.
				//int min = Math.min(left.names.size(), right.names.size());

				//for (int i = 0; i < min; i++) {
					// Если подразделения одного уровня не равны
				//	if (!left.names.get(i).equals(right.names.get(i))) {
					//	res = -1 * left.names.get(i).compareTo(right.names.get(i));
					//	break;
					//}
					// Если подразделения на всех уровнях равны, но длины списков разные
					// (т.е. последняя итерация и код не был прерван условием выше)
				//	if (i == min - 1 && !isLengthsEqual) {
					//	res = right.names.size() - left.names.size();
				//	}
			//}*/
			//	return res;
			//}

	//	}
	//}
//}
